/**
 * Claim Detector Agent
 * Placeholder for detecting and extracting claims from text
 */

export async function run(input: { text: string }) {
  return {
    status: "not implemented",
    input,
    placeholder: "This agent will detect factual claims from user input",
  };
}
