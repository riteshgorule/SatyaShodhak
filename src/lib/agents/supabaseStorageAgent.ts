/**
 * Supabase Storage Agent
 * Placeholder for storing verification results in Supabase
 */

export async function run(input: { userId: string; result: any }) {
  return {
    status: "not implemented",
    input,
    placeholder: "This agent will store verification results in Supabase database",
  };
}
