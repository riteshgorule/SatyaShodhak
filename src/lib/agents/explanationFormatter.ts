/**
 * Explanation Formatter Agent
 * Placeholder for formatting verification results into human-readable explanations
 */

export async function run(input: { verdict: string; evidence: any[] }) {
  return {
    status: "not implemented",
    input,
    placeholder: "This agent will format results into clear explanations",
  };
}
